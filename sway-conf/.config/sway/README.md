# Sway Config


## Installation
```bash
sudo apt install -y sway wofi
sudo apt install -y fonts-noto-color-emoji acpi curl
curl https://sh.rustup.rs -sSf | sh
git clone https://github.com/elkowar/ew
sudo apt install build-essentials pkg-config libgtk-3-dev libgtk-layer-shell*
sudo apt install -y pipewire-pulse pavucontrol
```
